[project]
name = "test-scheduler-bazel-demo"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
  "buildkite-sdk>=0.12.0",
  "httpx>=0.28.1",
]

[dependency-groups]
dev = [
  "buildkite-test-collector>=1.8.0",
  "pytest>=8.4.1",
]

[tool.uv]
package = false
